public class Util {
/*     public double reparar(String motor, int estado_motor, String clase){
        int multiplicador = multiplicadorMotor(motor);
        return calcularPrecio(clase, estado_motor, multiplicador);
    }

    private double calcularPrecio(String clase, int estado_motor, int multiplicador){
        if (clase.equals("Coche") ){ // Coche
            return ((10 - estado_motor)/2) * multiplicador;
        }
        if (clase.equals("Moto")){ // moto
            return ((10 - estado_motor)/2.5) * multiplicador;
        }
        if (clase.equals("Bici")){ // bici
            return ((10 - estado_motor)/3) * multiplicador;        
        }
        return 0;
    }



    public double cobrar(double numeroHoras){
        return numeroHoras * 23.5;
    } */
}